# Node / frontend
node_modules/
dist/
*.local

# Java / backend build output
target/

# H2 dev database & data dir
backend/data/

# Secrets — real deployment env, never commit (see .env.example for the template)
.env
.env.*
!.env.example

# Database backups — contain employee PII (Aadhaar, salary). Never commit.
backups/

# Logs
*.log

# OS / editor cruft
.DS_Store
*.swp

# Local Claude Code settings (keep launch.json shared)
.claude/settings.local.json

# Stale pre-split snapshot of the project, kept only as a local backup
niic-erp-main/
