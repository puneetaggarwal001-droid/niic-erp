package com.niic.erp.common;

// Shared status for the various admin-approval workflows (job requests,
// routing change requests, production edit requests).
public enum RequestStatus {
    PENDING, APPROVED, REJECTED
}
