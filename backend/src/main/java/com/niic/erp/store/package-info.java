/**
 * Phase 2. Will replace the legacy app's fstore_* keys: category/item master
 * (RM/SFG/FG), item variants, item approval history, RM/SFG/FG transactions,
 * BOM (including SFG-level BOM), purchase orders, stock bookings per job, and
 * issuance/transfer challans + material requisitions.
 */
package com.niic.erp.store;
